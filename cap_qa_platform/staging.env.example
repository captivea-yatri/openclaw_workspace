# Copy to staging.env and: set -a && source staging.env && set +a

ODOO_URL=http://localhost:1919/
ODOO_DB=odoo19_captivea2
ODOO_USER=admin1
ODOO_PASSWORD=change-me
ODOO_RPC=jsonrpc

# UI tests use cap_qa_tester after backend assigns role
CAP_QA_UI_HEADLESS=1

# Never on production unless intentional:
# CAP_QA_ALLOW_PRODUCTION=1
